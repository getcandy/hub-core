export const state = () => ({
    model: null,
})

export const mutations = {
    setModel (state, model) {
        state.model = model
    },
}

export const actions = {
}

export default {
    state,
    mutations,
    actions
}