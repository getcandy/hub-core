export const state = () => ({
  term: null,
  results: []
})
export const mutations = {
  setTerm (state, term) {
    // state.term = term
  },
  setResults (state, options) {
    // state.model.attribute_data[options.handle] = options.value
  }
}
