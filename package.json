{
  "name": "@getcandy/hub-core",
  "version": "0.12.0",
  "workspaces": [
    "./*"
  ],
  "private": true,
  "scripts": {
    "dev": "yarn workspace getcandy-hub dev",
    "bootstrap": "lerna bootstrap --hoist --stream"
  },
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
